#include <stdio.h>
#include <string.h>

/**
 * shared_lib_function
 * 
 * This function is defined in the shared library and is
 * program
 * 
 * @param msg: message to be printed
 * @return int: length of the message
*/
int shared_lib_function(char* msg);